import Counter from './ui/Counter';
import { CounterScheme } from './model/type/counterScheme';
import { counterReducer } from './model/slice/counterSlice';

export { Counter, CounterScheme, counterReducer };
